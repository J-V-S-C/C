/*- Criar um algoritmo que leia os elementos de uma matriz 10x10 e escreva todos os elementos,
exceto os elementos da diagonal principal.
*/

#include<stdio.h>
#include<locale.h>

main()

{
setlocale(LC_ALL, "portuguese");
int M[3][3], l=0, c=0;

    for (l=0;l<3;l++)
            for (c=0;c<3;c++)
            {
                printf("Digite um número:\\n");
                scanf("%d", &M[l][c]);
            }
    for (l=0;l<3;l++)
            for (c=0;c<3;c++)
            {
                if(l!=c)
                printf("os elementos da matriz ,exceto os da diagonal principal são:%d\\n", M[l][c]);
            }

}
